# Nexus-Code Daemon — Python Dependencies
# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pyyaml>=6.0.1
httpx>=0.27.0

# Caching & storage
faiss-cpu>=1.7.4
numpy>=1.24.0
aiosqlite>=0.19.0

# Token management & routing
tiktoken>=0.5.0
litellm>=1.40.0

# Graph / repo map
networkx>=3.1

# Prompt compression (lightweight, no torch)
# llmlingua is torch-heavy — replaced with tiktoken-based compression in code

# System
psutil>=5.9.0

# Tree-sitter for repo map (pure C, no torch)
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0

# NOTE: sentence-transformers (and torch) are OPTIONAL.
# If installed they unlock embedding-based intent routing.
# Install separately with: pip install sentence-transformers
# (requires ~2GB download including PyTorch)
